<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit Username</title>
  </head>
  <body>
    <h2>You're going to edit this user: <%= user.email %></h2>
    <p>Make it unique, else the databse will not register the new username.</p>
    <form action="/user/<%= user.id %>?_method=PATCH" method="post">
      <input name="username" placeholder="<%= user.username%>" />
      <input type="password" name="password" placeholder="enter password" />
      <button>Edit</button>
    </form>
  </body>
</html>
